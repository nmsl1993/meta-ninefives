# Nine Fives U-Boot Configuration Overrides

# Disable autoboot (stops at U-Boot prompt)
CONFIG_BOOTDELAY=-1

# Enable additional commands useful for debugging
CONFIG_CMD_GPIO=y
CONFIG_CMD_I2C=y
CONFIG_CMD_MEMTEST=y

# Custom boot command
CONFIG_BOOTCOMMAND="echo 'Nine Fives U-Boot Ready'; echo 'Type help for commands'"

# Enable verbose boot messages
CONFIG_LOGLEVEL=7

# Custom environment variables
CONFIG_EXTRA_ENV_SETTINGS="ninefives_version=1.0\0company=ninefives\0"